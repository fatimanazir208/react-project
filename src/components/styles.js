import { makeStyles } from "@material-ui/core";

export const styles = makeStyles((theme) => ({
  root: {
    margin: "20px 40px",
  },
  paper: {
    textAlign: "center",
    height: "100%",
    backgroundColor: "#f0f1e4",
  },
}));
